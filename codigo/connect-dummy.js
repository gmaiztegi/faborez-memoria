function middlewareConGranFuncionalidad(req, res, next) {
    // Llevar a cabo tareas necesarias con req (petición) y res (respuesta).
    // Una vez terminado, llamar a la función next() para dar paso al
    // siguiente middleware, en su caso.
    next();
}
